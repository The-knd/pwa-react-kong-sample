{
  "name": "pos-backend",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "common",
    "auth-service",
    "users-service",
    "clients-service",
    "sales-service"
  ],
  "scripts": {
    "build": "npm run build -w common && npm run build -w auth-service && npm run build -w users-service && npm run build -w clients-service && npm run build -w sales-service"
  },
  "engines": {
    "node": ">=20"
  }
}
