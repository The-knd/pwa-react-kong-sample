# ── Postgres raíz (solo para bootstrap de las 3 BDs) ─────────────
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_dev

# ── Usuario de aplicación (creado por db/init/01-databases.sh) ───
POS_DB_USER=pos_app
POS_DB_PASSWORD=pos_app_dev

# ── Cookies ──────────────────────────────────────────────────────
# true solo cuando haya HTTPS delante (producción)
COOKIE_SECURE=false
